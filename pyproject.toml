[project]
name = "archotraz"
version = "0.1.0"
description = "Evidence-first repository intake and recombination engine"
requires-python = ">=3.11"

[project.scripts]
archotraz = "archotraz.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
